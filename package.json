{
  "name": "trademe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TradeMe skilled-trade labor brokerage marketplace",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
